# This will search all the various zsh history files I now have
# It will take -A -B like grep to return blocks around history
# It will have variable output, depending on the need
# Apon request, it will return something suitable for c/p
